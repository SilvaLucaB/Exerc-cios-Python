# Desafio 48

# Soma dos multiplos de 3

# Escreva um programa que some todos os numeros impares que NÃO são multiplos de 3 de 1 até 500

soma = 0
cont = 0
for c in range(1, 501,2):
    if c % 3 == 0:
        soma = soma + c
        cont += 1
        
        
print(f"A soma dos todos os {cont} valores de 1 a 500 impares multiplos de 3 é {soma}")
